import mongoose from "mongoose";

    // profile references user
const ReviewSchema = new mongoose.Schema({
    //review string
    //rating number (max limit 5) (min limit 1)
    //profile id of person being reviewed
    //profile id of person who wrote the review
});